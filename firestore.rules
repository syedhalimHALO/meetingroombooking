rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() { return request.auth != null; }
    match /rooms/{roomId} { allow read: if signedIn(); allow write: if false; }
    match /bookings/{bookingId} { allow read: if signedIn(); allow write: if false; }
  }
}
